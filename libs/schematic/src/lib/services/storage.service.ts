import {Injectable} from '@angular/core';

// TODO: It's a temporary stub for the JSSdkLocalStorageService. Will be removed during the actual migration of the service.
@Injectable()
export class JSSdkLocalStorageService {
  setItem(x, y) {
    console.log(x, y);
  }
}
